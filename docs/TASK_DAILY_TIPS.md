# Задача: Реализация системы ежедневных советов по питанию

**Тип:** Feature
**Приоритет:** Medium
**Оценка:** 5-8 часов
**Статус:** TODO

---

## 📋 Описание

Реализовать систему персонализированных ежедневных советов по питанию, которые показываются пользователю при открытии приложения. Советы должны быть релевантны выбранной диете и цели пользователя.

**Бизнес-цель:**
- Повысить вовлеченность пользователей
- Обеспечить образовательную ценность приложения
- Мотивировать пользователей придерживаться выбранной диеты
- Увеличить время использования приложения

---

## 🎯 Требования

### Функциональные требования

#### 1. Выбор диеты пользователем

- [ ] Добавить поле `dietType` в модель User (Supabase)
- [ ] Добавить поле `showDailyTips` (boolean, по умолчанию true)
- [ ] Создать экран выбора диеты в OnboardingQuestionnaire
- [ ] Добавить возможность изменить диету в ProfileScreen

**Доступные типы диет:**
- Сбалансированное питание (balanced)
- Дефицит калорий (calorie_deficit)
- Кето-диета (keto)
- Низкоуглеводная (low_carb)
- Высокобелковая (high_protein)
- Средиземноморская (mediterranean)
- Интервальное голодание (intermittent_fasting)
- Палео (paleo)
- Веганская (vegan)
- Вегетарианская (vegetarian)

#### 2. Сервис для работы с советами

- [ ] Создать `nutritionTipsService.ts`
- [ ] Реализовать метод `getDailyTip(user: User): Promise<DailyTip | null>`
- [ ] Реализовать логику фильтрации по диете и цели
- [ ] Реализовать логику избежания повторов
- [ ] Хранить показанные советы в AsyncStorage

**Логика выбора совета:**
1. Проверить дату последнего показа
2. Если сегодня совет уже показывался - вернуть его
3. Если нет - выбрать случайный из подходящих
4. Фильтровать по `dietType` пользователя
5. Опционально фильтровать по `goalType`
6. Избегать советов, показанных за последние 30 дней
7. Сохранить в AsyncStorage

#### 3. UI компонент карточки совета

- [ ] Создать `DailyTipCard.tsx`
- [ ] Дизайн карточки:
  - Заголовок "💡 Совет дня"
  - Эмодзи совета (крупная)
  - Заголовок совета (жирный шрифт)
  - Текст совета
  - Кнопка "Закрыть" (скрывает карточку до следующего дня)
  - Опционально: кнопка "Подробнее" (открывает NUTRITION.md)
- [ ] Анимация появления (fade in + slide down)
- [ ] Поддержка светлой/темной темы

#### 4. Интеграция в DiaryScreen

- [ ] Показывать карточку вверху списка
- [ ] Загружать совет при монтировании экрана
- [ ] Сохранять состояние закрытия в локальном state
- [ ] При закрытии - скрывать до следующего дня

#### 5. Настройки

- [ ] Добавить в ProfileScreen переключатель "Показывать советы дня"
- [ ] При выключении - не показывать карточку
- [ ] Сохранять настройку в профиле пользователя (Supabase)

---

### Нефункциональные требования

- [ ] **Производительность:** Загрузка совета не должна блокировать UI
- [ ] **Кеширование:** Советы должны быть доступны оффлайн
- [ ] **Локализация:** Поддержка русского и английского языков
- [ ] **Типизация:** Строгая TypeScript типизация
- [ ] **Тестирование:** Unit тесты для сервиса
- [ ] **Аналитика:** Трекинг просмотров и взаимодействий

---

## 🏗️ Техническая архитектура

### 1. База данных (Supabase)

```sql
-- Миграция: 005_add_diet_type.sql
ALTER TABLE users ADD COLUMN diet_type TEXT;
ALTER TABLE users ADD COLUMN show_daily_tips BOOLEAN DEFAULT true;

-- Опционально: таблица для трекинга показов
CREATE TABLE daily_tips_shown (
  id UUID PRIMARY KEY DEFAULT uuid_generate_v4(),
  user_id UUID REFERENCES users(id) ON DELETE CASCADE,
  tip_id TEXT NOT NULL,
  shown_at TIMESTAMP DEFAULT NOW(),
  dismissed BOOLEAN DEFAULT false
);

CREATE INDEX idx_tips_shown_user_date ON daily_tips_shown(user_id, shown_at);
```

### 2. TypeScript типы

```typescript
// Уже реализовано в src/types/index.ts
export type DietType = 'balanced' | 'calorie_deficit' | 'keto' | ...;
export type TipCategory = 'nutrition' | 'hydration' | 'meal_timing' | ...;

export interface DailyTip {
  id: string;
  category: TipCategory;
  dietTypes: DietType[];
  goalTypes?: GoalType[];
  title: string;
  text: string;
  emoji?: string;
}

export interface User {
  // ... существующие поля
  dietType?: DietType;
  showDailyTips?: boolean;
}
```

### 3. Сервис (nutritionTipsService.ts)

```typescript
class NutritionTipsService {
  // Получить совет дня для пользователя
  async getDailyTip(user: User): Promise<DailyTip | null>

  // Получить все подходящие советы для пользователя
  getApplicableTips(user: User): DailyTip[]

  // Фильтровать советы по диете и цели
  filterTips(tips: DailyTip[], dietType: DietType, goalType?: GoalType): DailyTip[]

  // Получить показанные советы из AsyncStorage
  async getShownTips(userId: string): Promise<string[]>

  // Сохранить показанный совет
  async markTipAsShown(userId: string, tipId: string): Promise<void>

  // Отметить совет как закрытый
  async dismissTip(userId: string, tipId: string): Promise<void>

  // Проверить, показывался ли совет сегодня
  async wasTipShownToday(userId: string): Promise<boolean>
}

export default new NutritionTipsService();
```

### 4. Компонент (DailyTipCard.tsx)

```typescript
interface DailyTipCardProps {
  tip: DailyTip;
  onDismiss: () => void;
  onLearnMore?: () => void;
}

export default function DailyTipCard({ tip, onDismiss, onLearnMore }: DailyTipCardProps) {
  // Анимация появления
  // Рендер карточки
  // Обработчики событий
}
```

### 5. Интеграция в DiaryScreen

```typescript
export default function DiaryScreen({ userId, user }: DiaryScreenProps) {
  const [dailyTip, setDailyTip] = useState<DailyTip | null>(null);
  const [tipDismissed, setTipDismissed] = useState(false);

  useEffect(() => {
    const loadTip = async () => {
      if (user.showDailyTips && !tipDismissed) {
        const tip = await nutritionTipsService.getDailyTip(user);
        setDailyTip(tip);
      }
    };
    loadTip();
  }, [user]);

  const handleDismissTip = async () => {
    if (dailyTip) {
      await nutritionTipsService.dismissTip(userId, dailyTip.id);
      setTipDismissed(true);
      setDailyTip(null);
    }
  };

  return (
    <ScrollView>
      {dailyTip && !tipDismissed && (
        <DailyTipCard tip={dailyTip} onDismiss={handleDismissTip} />
      )}
      <StatsCard />
      {/* ... */}
    </ScrollView>
  );
}
```

---

## 📝 Этапы реализации

### Этап 1: База данных и типы (1-2 часа)

1. Создать миграцию Supabase для добавления полей
2. Обновить типы в `src/types/index.ts` (✅ Готово)
3. Обновить authService для сохранения dietType

### Этап 2: Экран выбора диеты (1-2 часа)

1. Добавить шаг в OnboardingQuestionnaire
2. Создать UI с карточками диет
3. Добавить описание каждой диеты
4. Сохранять выбор в профиле

### Этап 3: Сервис советов (2-3 часа)

1. Создать `nutritionTipsService.ts`
2. Реализовать логику фильтрации
3. Реализовать работу с AsyncStorage
4. Написать unit тесты
5. Интегрировать аналитику

### Этап 4: UI компонент (1-2 часа)

1. Создать `DailyTipCard.tsx`
2. Стилизация для светлой/темной темы
3. Анимации появления/скрытия
4. Адаптивность под разные размеры экрана

### Этап 5: Интеграция и настройки (1 час)

1. Интегрировать в DiaryScreen
2. Добавить переключатель в ProfileScreen
3. Тестирование на разных диетах
4. Проверка локализации

---

## ✅ Критерии приемки

### Обязательные

- [ ] Пользователь может выбрать тип диеты в онбординге
- [ ] При открытии DiaryScreen показывается совет, релевантный диете
- [ ] Один совет показывается один раз в день
- [ ] При закрытии совета он скрывается до следующего дня
- [ ] Советы не повторяются минимум 30 дней
- [ ] Пользователь может отключить советы в настройках
- [ ] Компонент корректно работает в светлой и темной теме
- [ ] Советы локализованы на русский и английский
- [ ] Нет утечек памяти и проблем с производительностью

### Желательные

- [ ] Анимация появления карточки
- [ ] Кнопка "Подробнее" открывает статью о диетах
- [ ] Аналитика: трекинг просмотров, закрытий, кликов
- [ ] Возможность оценить совет (👍/👎)
- [ ] История показанных советов в профиле

---

## 🎨 Дизайн

### Карточка совета (DailyTipCard)

```
┌─────────────────────────────────────────┐
│  💡 Совет дня                      [×]  │
├─────────────────────────────────────────┤
│                                         │
│            🥑 (большая эмодзи)          │
│                                         │
│       Жиры - ваш друг (заголовок)      │
│                                         │
│  На кето-диете 70-75% калорий должно   │
│  поступать из жиров. Добавьте авокадо, │
│  орехи или оливковое масло в рацион.   │
│                                         │
│           [Понятно] [Подробнее →]       │
└─────────────────────────────────────────┘
```

### Цветовая схема

**Светлая тема:**
- Фон карточки: `theme.surface` (белый/светло-серый)
- Заголовок: `theme.primary` (основной цвет)
- Текст: `theme.text`
- Граница: `theme.border`

**Темная тема:**
- Фон карточки: `theme.surface` (темно-серый)
- Заголовок: `theme.primary`
- Текст: `theme.text` (светлый)
- Граница: `theme.border`

---

## 🔍 Аналитика

Трекинг событий через `analyticsService`:

```typescript
// Показ совета
analyticsService.track('daily_tip_viewed', {
  tipId: tip.id,
  category: tip.category,
  dietType: user.dietType,
  goalType: user.goalType,
});

// Закрытие совета
analyticsService.track('daily_tip_dismissed', {
  tipId: tip.id,
  timeOnScreen: 15, // секунд
});

// Клик на "Подробнее"
analyticsService.track('daily_tip_learn_more_clicked', {
  tipId: tip.id,
});

// Отключение советов
analyticsService.track('daily_tips_disabled', {
  userId: user.id,
});
```

---

## 🧪 Тестирование

### Unit тесты (nutritionTipsService.test.ts)

```typescript
describe('NutritionTipsService', () => {
  it('должен вернуть совет для кето-диеты', async () => {
    const user = { dietType: 'keto', goalType: 'lose_weight' };
    const tip = await nutritionTipsService.getDailyTip(user);
    expect(tip).toBeDefined();
    expect(tip.dietTypes).toContain('keto');
  });

  it('должен не показывать один совет дважды подряд', async () => {
    // ...
  });

  it('должен фильтровать советы по цели', async () => {
    // ...
  });

  it('должен корректно работать с AsyncStorage', async () => {
    // ...
  });
});
```

### E2E тесты

- [ ] Пользователь проходит онбординг и выбирает диету
- [ ] Пользователь видит совет при открытии DiaryScreen
- [ ] Пользователь закрывает совет - он не показывается снова сегодня
- [ ] Пользователь отключает советы в настройках - они не показываются

---

## 📊 Метрики успеха

После запуска отслеживать:

1. **Вовлеченность:**
   - % пользователей, которые видят советы
   - Среднее время просмотра совета
   - % кликов на "Подробнее"

2. **Удержание:**
   - Влияние советов на DAU/MAU
   - Retention rate пользователей с советами vs без

3. **Контент:**
   - Топ-10 самых просмотренных советов
   - Топ-10 советов с самым высоким engagement
   - Категории с наибольшим интересом

4. **Отключения:**
   - % пользователей, отключивших советы
   - Причины отключения (если добавим опрос)

---

## 🚀 План деплоя

### Pre-release

1. Создать feature branch: `feature/daily-tips`
2. Реализовать функционал
3. Code review
4. Тестирование на staging
5. Миграция БД на staging

### Release

1. Merge в main
2. Создать миграцию для production
3. Деплой в production
4. Мониторинг ошибок (Sentry)
5. Анализ метрик

### Post-release

1. Собрать фидбек от первых 100 пользователей
2. Оптимизировать на основе данных
3. Добавить новые советы
4. Планировать улучшения (фаза 2)

---

## 🔮 Будущие улучшения (Phase 2)

1. **Умные советы**
   - Анализ дневника пользователя
   - Советы на основе недостающих макросов
   - Персонализация по времени суток

2. **Интерактивность**
   - Оценка полезности (👍/👎)
   - Избранные советы
   - Поделиться советом в соцсетях

3. **Прогрессивные советы**
   - Новички получают базовые советы
   - Опытные - продвинутые

4. **Тематические недели**
   - Неделя гидратации
   - Неделя белка
   - Неделя овощей

5. **Уведомления**
   - Push-уведомление с советом дня
   - Напоминания о воде на основе советов

---

## 📚 Связанные документы

- [DAILY_TIPS.md](DAILY_TIPS.md) - Архитектура системы советов
- [NUTRITION.md](NUTRITION.md) - База знаний по диетам
- [src/data/tips/README.md](../src/data/tips/README.md) - Управление контентом
- [src/data/tips/QUICK_START.md](../src/data/tips/QUICK_START.md) - Быстрый старт

---

## 👥 Ответственные

- **Product Owner:** [Имя]
- **Backend Developer:** [Имя] (миграция БД, API)
- **Frontend Developer:** [Имя] (UI компоненты)
- **Content Manager:** [Имя] (наполнение советами)
- **QA Engineer:** [Имя] (тестирование)
- **Designer:** [Имя] (UI/UX дизайн карточки)

---

## 📅 Timeline

| Этап | Срок | Статус |
|------|------|--------|
| База данных и типы | Day 1 | ⬜ TODO |
| Экран выбора диеты | Day 1-2 | ⬜ TODO |
| Сервис советов | Day 2-3 | ⬜ TODO |
| UI компонент | Day 3-4 | ⬜ TODO |
| Интеграция | Day 4 | ⬜ TODO |
| Тестирование | Day 5 | ⬜ TODO |
| Code review | Day 5 | ⬜ TODO |
| Deploy | Day 6 | ⬜ TODO |

**Общая оценка:** 5-6 рабочих дней (1 спринт)

---

## ⚠️ Риски и зависимости

### Риски

1. **Технические:**
   - Проблемы с производительностью при большом количестве советов
   - Конфликты с существующей логикой OnboardingQuestionnaire
   - Сложность локализации большого объема контента

2. **Продуктовые:**
   - Пользователи могут находить советы раздражающими
   - Низкая релевантность советов для некоторых диет
   - Быстрое выгорание контента (повторы)

3. **Ресурсные:**
   - Необходимость постоянного наполнения новыми советами
   - Нужен контент-менеджер для поддержки

### Зависимости

- Supabase доступен для миграций
- Дизайн карточки согласован с Product Owner
- Контент (70 советов) уже подготовлен ✅
- i18n настроен и работает

### Митигация

- Провести A/B тест с 20% пользователей
- Собрать фидбек перед full rollout
- Подготовить контент на 3 месяца вперед
- Мониторить метрику отключений

---

**Создано:** 2026-02-07
**Последнее обновление:** 2026-02-07
**Версия:** 1.0
