# Система ежедневных советов по питанию

## Описание

Система показывает пользователям ежедневные советы по питанию при открытии приложения. Советы персонализированы на основе выбранной диеты и цели пользователя.

## Архитектура

### 1. Типы диет (DietType)

```typescript
export type DietType =
  | 'balanced' // Сбалансированное питание
  | 'calorie_deficit' // Дефицит калорий
  | 'keto' // Кето-диета
  | 'low_carb' // Низкоуглеводная
  | 'high_protein' // Высокобелковая
  | 'mediterranean' // Средиземноморская
  | 'intermittent_fasting' // Интервальное голодание
  | 'paleo' // Палео
  | 'vegan' // Веганская
  | 'vegetarian'; // Вегетарианская
```

### 2. Структура совета

```typescript
export interface DailyTip {
  id: string;
  category: 'nutrition' | 'hydration' | 'meal_timing' | 'food_benefits' | 'general';
  dietTypes: DietType[]; // Для каких диет подходит
  goalTypes?: GoalType[]; // Для каких целей (опционально)
  title: string; // Заголовок совета
  text: string; // Текст совета
  emoji?: string; // Эмодзи для визуала
  action?: {
    // Опциональное действие
    type: 'track_water' | 'add_meal' | 'view_profile';
    label: string;
  };
}
```

### 3. Категории советов

- **nutrition** - Советы по питанию и рациону
- **hydration** - Советы по потреблению воды
- **meal_timing** - Советы по времени приема пищи
- **food_benefits** - Польза конкретных продуктов
- **general** - Общие советы по здоровью

## Структура файлов

```
src/
├── data/
│   └── nutritionTips.ts         # База данных советов
├── services/
│   └── nutritionTipsService.ts  # Логика выбора советов
├── components/
│   └── DailyTipCard.tsx         # UI компонент
└── types/
    └── index.ts                  # Типы DietType, DailyTip
```

## Логика работы

### 1. Выбор совета

```typescript
// При открытии приложения
const tip = await nutritionTipsService.getDailyTip(user);
```

**Алгоритм:**

1. Получить дату и проверить, показывался ли сегодня совет
2. Если нет - выбрать случайный совет для диеты пользователя
3. Сохранить в AsyncStorage, чтобы не показывать повторно
4. Вернуть совет

### 2. Фильтрация советов

Советы фильтруются по:

- **Типу диеты** (обязательно)
- **Цели пользователя** (опционально)
- **Показанным ранее** (избегать повторов)

### 3. Хранение состояния

```typescript
AsyncStorage.setItem(
  'daily_tip_shown',
  JSON.stringify({
    date: '2026-02-07',
    tipId: 'tip_001',
  })
);
```

## Примеры советов

### Кето-диета

```typescript
{
  id: 'keto_001',
  category: 'nutrition',
  dietTypes: ['keto', 'low_carb'],
  goalTypes: ['lose_weight'],
  title: 'Жиры - ваш друг',
  text: 'На кето-диете 70-75% калорий должно поступать из жиров. Добавьте авокадо, орехи или оливковое масло в рацион.',
  emoji: '🥑',
}
```

### Высокобелковая диета

```typescript
{
  id: 'protein_001',
  category: 'nutrition',
  dietTypes: ['high_protein'],
  goalTypes: ['gain_weight'],
  title: 'Белок после тренировки',
  text: 'Употребите 20-40г белка в течение 2 часов после силовой тренировки для максимального роста мышц.',
  emoji: '💪',
}
```

### Интервальное голодание

```typescript
{
  id: 'if_001',
  category: 'meal_timing',
  dietTypes: ['intermittent_fasting'],
  title: 'Первый прием пищи',
  text: 'Начинайте "окно питания" с богатой белком еды - это снизит аппетит и поможет контролировать калории.',
  emoji: '⏰',
}
```

### Сбалансированное питание

```typescript
{
  id: 'balanced_001',
  category: 'food_benefits',
  dietTypes: ['balanced', 'mediterranean'],
  title: 'Цветная тарелка',
  text: 'Старайтесь, чтобы ваша тарелка содержала продукты разных цветов - это гарантирует разнообразие витаминов.',
  emoji: '🌈',
}
```

### Гидратация (для всех)

```typescript
{
  id: 'hydration_001',
  category: 'hydration',
  dietTypes: ['balanced', 'keto', 'low_carb', 'high_protein', 'mediterranean', 'intermittent_fasting', 'paleo', 'vegan', 'vegetarian'],
  title: 'Вода утром',
  text: 'Выпейте стакан воды сразу после пробуждения - это запустит метаболизм и восстановит гидратацию после сна.',
  emoji: '💧',
  action: {
    type: 'track_water',
    label: 'Отметить воду',
  },
}
```

## UI/UX

### Расположение

**Вариант 1: Карточка в начале DiaryScreen**

- Отображается вверху экрана дневника
- Можно свернуть/закрыть
- Обновляется ежедневно

**Вариант 2: Модальное окно при запуске**

- Показывается при первом открытии за день
- Можно закрыть или пропустить
- Менее навязчиво

**Рекомендация:** Вариант 1 - карточка на экране дневника

### Дизайн компонента

```
┌─────────────────────────────────────┐
│  💡  Совет дня                      │
├─────────────────────────────────────┤
│                                     │
│  🥑  Жиры - ваш друг               │
│                                     │
│  На кето-диете 70-75% калорий      │
│  должно поступать из жиров.        │
│  Добавьте авокадо, орехи или       │
│  оливковое масло в рацион.         │
│                                     │
│  [Понятно]          [Подробнее →]  │
└─────────────────────────────────────┘
```

## Настройки для пользователя

### Добавить в профиль

```typescript
interface User {
  // ... существующие поля
  dietType?: DietType; // Тип диеты
  showDailyTips?: boolean; // Показывать советы (по умолчанию true)
}
```

### Экран выбора диеты

Добавить шаг в OnboardingQuestionnaire или в ProfileScreen:

```
Выберите тип питания:
□ Сбалансированное питание
□ Кето-диета
□ Низкоуглеводная диета
□ Высокобелковая диета
□ Интервальное голодание
□ Средиземноморская диета
□ Палео
□ Веганская
□ Вегетарианская
```

## Расширение системы

### Добавление новых советов

1. Откройте `src/data/nutritionTips.ts`
2. Добавьте новый объект совета в массив `NUTRITION_TIPS`
3. Укажите категорию, типы диет и текст
4. Советы автоматически попадут в ротацию

### Локализация

Советы должны поддерживать мультиязычность через i18n:

```typescript
// src/locales/en.ts
tips: {
  keto_001_title: 'Fats are your friend',
  keto_001_text: 'On keto, 70-75% of calories should come from fats...',
}

// src/locales/ru.ts
tips: {
  keto_001_title: 'Жиры - ваш друг',
  keto_001_text: 'На кето-диете 70-75% калорий должно поступать из жиров...',
}
```

## Аналитика

Отслеживать:

- Количество просмотренных советов
- Нажатия на "Подробнее"
- Выполнение действий (track_water, add_meal)
- Популярность категорий советов

```typescript
analyticsService.track('daily_tip_viewed', {
  tipId: tip.id,
  category: tip.category,
  dietType: user.dietType,
});
```

## Будущие улучшения

1. **Умные советы**
   - Анализ дневника пользователя
   - Советы на основе недостающих макросов
   - Персонализация по времени суток

2. **Интерактивность**
   - Оценка полезности совета (👍/👎)
   - Избранные советы
   - Поделиться советом

3. **Прогрессивные советы**
   - Новички получают базовые советы
   - Опытные пользователи - продвинутые

4. **Тематические недели**
   - Неделя гидратации
   - Неделя белка
   - Неделя овощей

## Миграция базы данных

При добавлении `dietType` в User:

```sql
-- Supabase migration
ALTER TABLE users ADD COLUMN diet_type TEXT;
ALTER TABLE users ADD COLUMN show_daily_tips BOOLEAN DEFAULT true;
```

## Пример использования

```typescript
// DiaryScreen.tsx
import { useState, useEffect } from 'react';
import DailyTipCard from '../components/DailyTipCard';
import nutritionTipsService from '../services/nutritionTipsService';

function DiaryScreen({ user }) {
  const [dailyTip, setDailyTip] = useState(null);

  useEffect(() => {
    const loadTip = async () => {
      const tip = await nutritionTipsService.getDailyTip(user);
      setDailyTip(tip);
    };
    loadTip();
  }, []);

  return (
    <ScrollView>
      {dailyTip && <DailyTipCard tip={dailyTip} onDismiss={() => setDailyTip(null)} />}
      <StatsCard />
      {/* ... */}
    </ScrollView>
  );
}
```

## Контент-стратегия

### Количество советов

- Минимум: 30 советов на диету (1 месяц без повторов)
- Оптимально: 90 советов на диету (3 месяца)
- Цель: 180 советов на диету (6 месяцев)

### Тон и стиль

- **Позитивный и мотивирующий**
- **Практичный и действенный**
- **Короткий (2-3 предложения)**
- **Научно обоснованный**
- **Без запугивания**

### Примеры хороших советов

✅ "Добавьте 30г орехов в дневной рацион - они богаты омега-3 и помогут контролировать аппетит"
✅ "Готовьте овощи на пару вместо варки - так сохраняется больше витаминов"
✅ "Ешьте медленно и осознанно - так вы почувствуете насыщение быстрее"

### Примеры плохих советов

❌ "Не ешьте после 18:00" (слишком категорично)
❌ "Откажитесь от сахара навсегда" (нереалистично)
❌ "Если вы едите углеводы, вы не похудеете" (ложная информация)

---

**Версия:** 1.0
**Дата создания:** 2026-02-07
**Статус:** В разработке
